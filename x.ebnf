(*
let world = 'world'
let a = :ok;
print("Hello, {world}")
inspect(match a {
    :ok => "ok",
    :notok => "not ok"
})
*)
sc = ";";

atom = stratom | ifatom;

(* prec climber *)
expr = prec_climb[* / - +] atom;

letstmt = kw:"let" ident:* "=" expr;
exprstmt = expr;
stmt = sc | letstmt | exprstmt;
program = [^eof](stmt program);